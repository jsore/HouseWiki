/**
 * I've given up trying to get this placement correct in
 * Django's blocks, this is not an overly essential site but
 * needs to get launched fast, this can be fixed later
 */

const hackToFixDivPlacement = () => {
  // const childList = document.getElementById('list-ul-container-id');
  // const parentDiv = document.getElementById('question-list-container');
  // const paginateDiv = document.getElementById('question-pages');

  // parentDiv.appendChild(childList);
  // parentDiv.appendChild(paginateDiv);
};

window.onload = hackToFixDivPlacement();
